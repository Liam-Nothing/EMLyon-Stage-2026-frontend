A AJOUTER :
- message attention comme quoi pas sauvergarder

- mettre tout en Francais

- pour page de présentation :
  • Back-End
  • required style

  // optionnel
  • Welcome msg


PROBLEME :
- dans admin :
  • Une fois save changes, le link se met en style desactiver tout comme le checkbox mais rest active
  • Qd click annuler, apres avoir clicker sur save, le checkbox se met en desactive (mais le style reste active) mais rest active

  // les moins important (petit détail) :
  • une fois ajouté un lien avec une icone, ne se déselectionne pas automatiquement
  • Une fois ajout d'un lien (que se soit save ou discard mais qu'on a rempli les input), lors d'un autre ajout, les input reste vert ou rouge (les couleur reste)
  • lorsqu'on essai de cliqué sur ajouté un lien sans avoir rempli whats requiere, ya plus le petit msg d'info

- dans design :
  • pb avec theme : automatically select a theme that isn't put

  // moins important
  • boutton color se met en noir lorsqu'on change le style 'solid'-'outline'

- dans preview card :
  • dans profil admin, une fois annuler, le texte modifier reste (jusqua ce quon actualise)
  • previewCard max-width is a pb when no bio
  • can't have empty bio